.col-md-12
  .category-item
    h2 = democrat_category.name
    img src=image_path("democrat_#{democrat_category.name.downcase}.jpg")
    ul
      - democrat_category.articles.by_party(:democrats).each do |article|
        .col-md-6
          li = render partial: "article", locals: { article: article }
